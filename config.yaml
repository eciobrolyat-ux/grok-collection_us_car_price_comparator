# RapidAPI key is read from the RAPIDAPI_KEY environment variable.
# (Optionally set rapidapi_key here for local dev, but never commit a real key.)
regions:
  washington_dc:
    zip: "20001"
    radius: 100
    name: "Washington DC"
  houston:
    zip: "77002"
    radius: 100
    name: "Houston TX"

targets:
  - year: 2015
    make: Chevrolet
    model: Corvette
