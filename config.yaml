# rapidapi_key lives in your local keybank vault, not here.
# Run: python keybank.py init && python keybank.py set rapidapi_key
regions:
  washington_dc:
    zip: "20001"
    radius: 100
    name: "Washington DC"
  houston:
    zip: "77002"
    radius: 100
    name: "Houston TX"

targets:
  - year: 2015
    make: Chevrolet
    model: Corvette
